<Window x:Class="costats.App.UpdateAvailableWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:costats.App"
        Title="Update available"
        Width="380"
        SizeToContent="Height"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0"
                      ResizeBorderThickness="0"
                      CornerRadius="14"
                      GlassFrameThickness="-1"
                      UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonBgBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="16,8" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource ButtonHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource ButtonPressedBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryDialogButton" TargetType="Button" BasedOn="{StaticResource DialogButton}">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource AccentTextBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
    </Window.Resources>

    <Border CornerRadius="14"
            BorderBrush="{DynamicResource WindowBorderBrush}"
            BorderThickness="1"
            Background="{DynamicResource PanelBgBrush}">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,0,0,16">
                <Button Content="X" Click="OnCloseClick" DockPanel.Dock="Right"
                        HorizontalAlignment="Right" Width="28" Height="28" Padding="0"
                        Style="{StaticResource DialogButton}" FontWeight="SemiBold" />
                <TextBlock Text="Update available" FontSize="18" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center" />
            </DockPanel>

            <Border Grid.Row="1" Height="1" Background="{DynamicResource DividerBrush}" Margin="0,0,0,16" />

            <StackPanel Grid.Row="2">
                <TextBlock Text="{Binding AvailableUpdateVersion, StringFormat='Version {0} is ready'}"
                           FontSize="14" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimaryBrush}" />
                <TextBlock Text="Review what changed, then update now or come back later."
                           FontSize="11" Foreground="{DynamicResource TextFaintBrush}"
                           TextWrapping="Wrap" Margin="0,5,0,16" />
                <TextBlock Text="WHAT'S NEW" FontSize="9" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,0,6" />
                <Border BorderBrush="{DynamicResource DividerBrush}" BorderThickness="1"
                        Background="{DynamicResource InputBgBrush}" CornerRadius="8" Padding="10">
                    <TextBox Text="{Binding AvailableUpdateNotes, Mode=OneWay}"
                             IsReadOnly="True" IsReadOnlyCaretVisible="True"
                             Background="Transparent" BorderThickness="0" Padding="0"
                             Foreground="{DynamicResource TextSecondaryBrush}"
                             FontSize="11" TextWrapping="Wrap" MinHeight="90" MaxHeight="220"
                             VerticalScrollBarVisibility="Auto" />
                </Border>
            </StackPanel>

            <TextBlock Grid.Row="3" Text="{Binding UpdateStatusText}"
                       FontSize="11" Foreground="{DynamicResource TextFaintBrush}"
                       TextWrapping="Wrap" Margin="0,12,0,0" />

            <ProgressBar Grid.Row="4" Height="5" Minimum="0" Maximum="100"
                         Value="{Binding UpdateProgressPercent}"
                         IsIndeterminate="{Binding IsUpdateProgressIndeterminate}"
                         Foreground="{DynamicResource AccentBrush}"
                         Background="{DynamicResource InputBgBrush}"
                         BorderThickness="0" Margin="0,8,0,0"
                         Visibility="{Binding IsUpdateProgressVisible, Converter={x:Static local:BoolToVisibilityConverter.Instance}}" />

            <DockPanel Grid.Row="5" Margin="0,18,0,0">
                <Button Content="Later" Click="OnLaterClick" IsCancel="True"
                        Style="{StaticResource DialogButton}" DockPanel.Dock="Left" />
                <Button Content="Update now" Command="{Binding InstallUpdateCommand}"
                        IsDefault="True"
                        IsEnabled="{Binding IsUpdateBusy, Converter={x:Static local:BoolNegationConverter.Instance}}"
                        Style="{StaticResource PrimaryDialogButton}"
                        DockPanel.Dock="Right" HorizontalAlignment="Right" />
            </DockPanel>
        </Grid>
    </Border>
</Window>
