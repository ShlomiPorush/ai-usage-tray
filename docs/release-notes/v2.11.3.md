## What's changed

A weekly quota reset now produces one notification per account. Previously an account with a model-specific weekly limit notified twice for the same reset, once for the overall weekly window and once for the model window, on Windows and in the browser alike.

A model-specific weekly window that resets on its own still notifies as before.
