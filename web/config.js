// Point this at the remote-view worker that stores the snapshots.
// No trailing slash; the page requests {apiBase}/u/{id}.
window.REMOTE_VIEW_CONFIG = { apiBase: "https://REPLACE-WITH-YOUR-WORKER-URL" };
